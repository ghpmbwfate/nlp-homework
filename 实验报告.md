# 基于大语言模型RAG的多模态金融报告问答系统——实验报告

## 一、项目背景

### 1.1 问题提出

在信息爆炸时代，知识不再局限于纯文本形式，而是广泛存在于图文混排的文档、网页、报告和图库中。传统的纯文本问答系统难以应对需要视觉推理的问题，例如"图表中哪个季度产品A销量开始下滑？"或"左图中设备的功能是什么？"。将检索增强生成（Retrieval-Augmented Generation, RAG）扩展到多模态领域，意味着模型能够从大规模多模态知识库中检索相关图像和文本片段，并综合生成准确答案。

### 1.2 任务描述

本项目要求实现一个基于大语言模型RAG的框架，完成以下四项核心任务：

1. **多模态信息理解**：同时理解自然语言问题和知识库中的图像与文本内容
2. **跨模态检索**：从多个PDF文档中高效检索相关图像、图表、文本段落或其组合
3. **图文关联推理**：有效关联和融合检索到的图像和文本信息，进行深层逻辑推理
4. **答案生成**：生成准确、简洁、上下文相关的答案，并明确标注信息来源（哪份PDF、哪一页、哪个图表）

### 1.3 数据集

- **知识库**：6份金融行业深度研究报告PDF，包含伊利股份、凌云股份、千味央厨、广联达（两份）、联邦制药，共计307页，涵盖大量图表、表格和混合排版内容
- **测试集**：67道问答题（覆盖6份报告），每题需预测`filename`（来源PDF）、`page`（页码）和`answer`（答案文本）

### 1.4 评分标准

| 评估维度 | 权重 | 说明 |
|----------|------|------|
| 页面匹配度 | 0.2 | 预测页码是否与标准答案一致 |
| 文件名匹配度 | 0.2 | 预测来源文件是否与标准答案一致 |
| 答案内容相似度 | 0.6 | 基于ROUGE-1、ROUGE-2、BLEU计算预测答案与标准答案的相似度 |

综合得分 = 0.2 × 页面匹配率 + 0.2 × 文件名匹配率 + 0.6 × 答案内容相似度（ROUGE-1 recall）

## 二、技术路线

### 2.1 系统架构总览

```
PDF报告
  ├── PyMuPDF (fitz) ──────────► page_content.json (纯文本逐页)
  └── VLM (qwen-vl-max) ───────► chart_descriptions.json (图表/表格结构化描述)
                                    ↓
               语义分块 → ChromaDB (bge-m3) + BM25 (jieba) 双索引
                                    ↓
问题 → 分类(4类) → dense + BM25 → RRF融合 → CrossEncoder重排 → top-k
                                    ↓
              API LLM (GPT-OSS-20B-BF16) → 答案 + 引用标注
```

系统分为三大模块：**PDF解析与多模态理解**、**混合检索与重排**、**答案生成与后处理**。

### 2.2 模块一：PDF解析与多模态理解

#### 2.2.1 纯文本提取（PyMuPDF）

由于Windows环境下MinerU无法正常工作（依赖detectron2，与CUDA 12.6不兼容），系统采用PyMuPDF（fitz）作为PDF文本提取的备选方案，逐页提取纯文本内容，输出`page_content.json`。

关键代码：`src/parsing/pdf_parser.py`

#### 2.2.2 视觉语言模型图表提取（VLM）

对于PDF中的图表、表格等视觉内容，使用DashScope API调用`qwen-vl-max`视觉语言模型，逐页进行结构化描述提取。每页输出包含：

- **文本块**（text blocks）：页面中的文字段落
- **图表元数据**（chart metadata）：标题、坐标轴、关键数据点
- **表格数据**（table data）：结构化的行列数据

VLM输出比纯文本更丰富，能保留图表的语义信息。支持增量处理（per_pdf保存），避免重复调用API。

关键代码：`src/parsing/chart_extractor.py`

#### 2.2.3 双层数据融合

系统维护两个数据层：

| 数据层 | 来源 | 内容 | 用途 |
|--------|------|------|------|
| `chart_descriptions.json` | VLM | 结构化图表/表格描述 | 主要检索源，语义更丰富 |
| `page_content.json` | PyMuPDF | 纯文本逐页 | 快速基线和索引构建的备选 |

### 2.3 模块二：混合检索与重排

#### 2.3.1 索引构建

**稠密索引**：使用`BAAI/bge-m3`多语言嵌入模型，通过ChromaDB构建向量索引。bge-m3支持多语言和多粒度嵌入，适合中文金融文档。

**稀疏索引**：使用jieba分词 + BM25算法构建词频索引，捕捉精确词项匹配信号。

**语义分块**：`src/indexing/chunking.py`实现了基于语义相似度的滑动窗口分块策略，根据句子间相似度自动寻找断点，支持图表描述的特殊处理（将图表元数据与正文分离为独立chunk）。

#### 2.3.2 检索策略

系统实现了8种检索器配置，均继承自`BaseRetriever`抽象基类：

| 检索器 | 策略 | 文件 |
|--------|------|------|
| TF-IDF | jieba + sklearn TfidfVectorizer | `src/baseline/tfidf.py` |
| BM25 | jieba + BM25词频匹配 | `src/baseline/bm25.py` |
| Dense | ChromaDB向量检索 | `src/baseline/dense.py` |
| BM25+Rerank | BM25 + CrossEncoder重排 | `src/baseline/bm25_rerank.py` |
| Dense+Rerank | 稠密检索 + CrossEncoder重排 | `src/baseline/dense_rerank.py` |
| Hybrid RRF | Dense + BM25 → RRF融合 | `src/baseline/hybrid_rrf.py` |
| **VRAG (Hybrid+CE)** | Dense + BM25 → RRF → CrossEncoder重排 | `src/retrieval/retriever.py` |

#### 2.3.3 倒数秩融合（RRF）

对稠密检索和稀疏检索的结果进行融合，采用RRF公式：

$$\text{RRF}(d) = \sum_{r \in R} \frac{1}{k + \text{rank}_r(d)}$$

其中 $k=60$ 为平滑常数，$R$ 为检索通道集合。支持5个通道：dense、bm25、title、keyword、summary。

#### 2.3.4 多阶段重排

可选的多阶段重排流水线：

1. **CrossEncoder精排**：使用`BAAI/bge-reranker-large`对粗排结果逐对打分
2. **MMR多样性重排**：最大边际相关性算法，避免结果高度相似
3. **问题类型过滤**：根据问题分类优先选择特定类型的内容（如chart_understanding优先选图表chunk）

系统还支持多种重排器后端：`BGEReranker`（sentence_transformers.CrossEncoder）和`QwenReranker`（AutoModelForCausalLM yes/no logits评分）。

#### 2.3.5 多路召回增强

除主要检索通道外，还构建了3个辅助索引：

- **标题搜索**（title_search）：BM25索引，基于提取的章节标题
- **关键词搜索**（keyword_search）：TF-IDF索引，基于分词后的页面文本
- **摘要搜索**（summary_search）：BM25索引，基于每页首句

#### 2.3.6 查询改写

`src/retrieval/query_rewriting.py`使用DashScope LLM进行查询改写、HyDE（假设性文档嵌入）和查询分解，优化检索效果。当API不可用时回退为NoOpQueryRewriter。

### 2.4 模块三：答案生成与后处理

#### 2.4.1 问题分类

基于关键词模式的问题分类器，将问题分为4类：

| 类型 | 描述 | 典型问题 |
|------|------|----------|
| fact_extraction | 事实提取 | "某公司2024年营业收入是多少？" |
| analysis_summary | 分析综述 | "如何分析某公司的增长趋势？" |
| chart_understanding | 图表理解 | "根据图表X，某指标变化趋势如何？" |
| comparison | 对比分析 | "A与B在哪些方面有差异？" |

#### 2.4.2 提示词工程

每种问题类型对应专门的提示词模板（v1和v2两个版本），v2版本引入了：

- 长度预算约束（100-400字符）
- 编号结构化输出
- 1-shot示例
- 引用格式要求

#### 2.4.3 Self-RAG一致性检查

`src/generation/self_rag.py`实现了基于规则的自检模块：

- **支持度判定**：基于数字重叠率和关键词（jieba）重叠率判断答案是否被上下文支持
- **自一致性投票**：Jaccard相似度多数投票
- **检索反馈**：根据判定结果给出改进建议

#### 2.4.4 后处理

`src/generation/postprocess.py`提供数字验证、格式规范化、空答案处理、长度控制等后处理功能。

## 三、实验过程

### 3.1 环境搭建

- **操作系统**：Windows 11 + CUDA 12.6
- **Python环境**：Conda `nlp` 环境
- **关键依赖**：PyMuPDF, ChromaDB, sentence-transformers, jieba, scikit-learn, DashScope API, OpenAI-compatible API
- **嵌入模型**：BAAI/bge-m3（通过ModelScope本地缓存）
- **重排模型**：BAAI/bge-reranker-large / bge-reranker-v2-m3 / Qwen3-0.6B
- **生成模型**：GPT-OSS-20B-BF16（通过OpenAI兼容API）
- **VLM模型**：qwen-vl-max（通过DashScope API）

### 3.2 数据处理流程

1. **PDF解析**：对6份PDF报告执行PyMuPDF文本提取，生成`page_content.json`（307页）
2. **VLM图表提取**：逐PDF调用qwen-vl-max，生成6份`chart_descriptions.json`
3. **索引构建**：基于VLM输出的结构化数据，执行语义分块并构建ChromaDB稠密索引和BM25稀疏索引
4. **多路召回索引**：构建标题、关键词、摘要三个辅助BM25索引

### 3.3 检索策略对比实验

对所有8种检索器配置进行端到端测试，使用67道测试题进行评估。

### 3.4 重排器消融实验

对比三种CrossEncoder重排器：

- `bge-reranker-large`（默认）
- `bge-reranker-v2-m3`
- `Qwen3-0.6B`（基于因果语言模型的yes/no评分）

### 3.5 提示词版本对比

对比v1和v2两个版本的提示词模板效果。

## 四、实验结果与分析

### 4.1 检索策略全量对比

| 检索器 | ROUGE-1 | ROUGE-2 | ROUGE-L | BLEU | 文件名准确率 | 页面准确率 | 引用准确率 | 综合得分 |
|--------|---------|---------|---------|------|-------------|-----------|-----------|---------|
| TF-IDF v2 | 0.2147 | 0.1248 | 0.2006 | 0.0660 | 0.8806 | 0.5224 | 0.5075 | 0.4094 |
| TF-IDF v3 | 0.4418 | 0.2093 | 0.3958 | 0.0936 | 0.8806 | 0.5224 | 0.5075 | 0.5457 |
| BM25 | 0.2233 | 0.1169 | 0.2114 | 0.0671 | 0.8955 | 0.6119 | 0.6119 | 0.4355 |
| BM25 v2 | 0.4901 | 0.2397 | 0.4221 | 0.1055 | 0.8955 | 0.6119 | 0.6119 | 0.5956 |
| **BM25+Rerank** | 0.5434 | 0.2637 | 0.4601 | 0.1166 | 0.9403 | 0.7463 | 0.7463 | **0.6634** |
| Dense | 0.5037 | 0.2529 | 0.4293 | 0.1104 | 0.9552 | 0.5970 | 0.5970 | 0.6127 |
| **Dense+Rerank** | **0.5348** | **0.2567** | **0.4572** | 0.1094 | **0.9552** | **0.8060** | **0.8060** | **0.6731** |
| Hybrid RRF | 0.5170 | 0.2515 | 0.4412 | **0.1123** | 0.9552 | 0.6567 | 0.6567 | 0.6326 |
| VRAG (bge-large) | 0.5214 | 0.2495 | 0.4404 | 0.1045 | 0.9552 | 0.8060 | 0.8060 | 0.6651 |
| VRAG (bge-v2-m3) | 0.5309 | 0.2645 | 0.4614 | 0.1171 | 0.9403 | 0.7761 | 0.7761 | 0.6619 |
| VRAG (qwen3-0.6b) | 0.5382 | 0.2575 | 0.4537 | 0.1166 | 0.9403 | 0.7910 | 0.7910 | 0.6693 |

> 综合得分 = 0.2 × 页面准确率 + 0.2 × 文件名准确率 + 0.6 × ROUGE-1 recall

### 4.2 关键发现

#### 4.2.1 CrossEncoder重排是最关键的提升因素

对比有无重排的差异：

| 对比组 | ROUGE-1提升 | 页面准确率提升 | 综合得分提升 |
|--------|------------|---------------|-------------|
| BM25 → BM25+Rerank | +0.3201 (+143%) | +0.1344 (+22%) | +0.2279 (+52%) |
| Dense → Dense+Rerank | +0.0311 (+6%) | +0.2090 (+35%) | +0.0604 (+10%) |
| Hybrid RRF → VRAG (bge-large) | +0.0044 (+1%) | +0.1493 (+23%) | +0.0325 (+5%) |

CrossEncoder重排对页面定位准确率的提升尤为显著，尤其是对Dense检索器（+35%）和Hybrid RRF（+23%），说明重排能有效筛选出与问题最相关的页面。

#### 4.2.2 稠密检索优于稀疏检索

在无重排条件下，Dense（ROUGE-1=0.5037）显著优于BM25（0.2233）和TF-IDF（0.2147），表明bge-m3嵌入模型在中文金融文档上的语义匹配能力远超词频匹配。加入重排后，BM25+Rerank（0.5434）反超Dense+Rerank（0.5348），说明BM25的精确匹配信号经重排后可被有效利用。

#### 4.2.3 混合检索不一定优于单路检索

Hybrid RRF（0.6326）的综合得分反而低于Dense+Rerank（0.6731）和BM25+Rerank（0.6634），可能原因：

- RRF融合的默认参数未针对本数据集调优
- Dense和BM25的检索结果存在大量重叠，融合增益有限
- 融合后候选集质量参差不齐，增加了重排器的负担

#### 4.2.4 重排器选择的影响

三种重排器在VRAG框架下的表现接近：

| 重排器 | ROUGE-1 | 页面准确率 | 综合得分 |
|--------|---------|-----------|---------|
| bge-reranker-large | 0.5214 | 0.8060 | 0.6651 |
| bge-reranker-v2-m3 | 0.5309 | 0.7761 | 0.6619 |
| Qwen3-0.6B | 0.5382 | 0.7910 | 0.6693 |

Qwen3-0.6B在ROUGE-1上略优，但页面准确率低于bge-large。bge-large在页面定位上最准确（0.8060），对综合得分贡献最大。

#### 4.2.5 文件名定位高度准确，页面定位是主要瓶颈

所有配置的文件名准确率均在88%-96%之间，说明检索器能正确定位到来源PDF。但页面准确率波动较大（52%-81%），是多模态PDF问答的核心挑战——图表和表格的语义信息在纯文本表示中损失严重，导致检索时难以精确定位到正确页面。

### 4.3 典型案例分析

**案例1：图表理解题表现良好**

问题："根据图表88，PC装配式建筑相较于现场浇筑建筑在哪些方面表现出优势？"

VRAG系统正确检索到广联达报告第52页（图表88所在页），并准确提取了6个维度的对比数据（工期、人力、水资源、能源、废弃物、粉尘），ROUGE-1达到0.32-0.48，答案内容与标准答案高度相关。

**案例2：数字提取题存在精度问题**

问题："根据联邦制药2024年的营业收入和净利润分别是多少？"

标准答案位于第22页的财务预测表（表4），但系统检索到第1页或第6页的概览数据。由于概览页的净利润数据（2,660百万元）与详细财务表（6,169百万元）不同，导致答案不准确。这反映了多页PDF中相同实体在不同表格中数据不一致时的定位困难。

**案例3：VLM增强的图表题**

问题："关于凌云股份的热冲压技术与冷冲压技术的主要区别？"

系统通过VLM提取的图表6描述，准确检索到第9页并生成了详细的对比表格，ROUGE-1达到0.75-0.83。VLM的结构化图表描述使检索器能够理解图表语义，大幅提升了图表理解类问题的回答质量。

### 4.4 各问题类型表现分析

| 问题类型 | 典型ROUGE-1 | 主要挑战 |
|----------|------------|----------|
| fact_extraction | 0.27-0.62 | 数字精确提取困难，多表数据不一致 |
| analysis_summary | 0.28-0.75 | 需要跨页面综合推理，答案冗长导致召回率偏低 |
| chart_understanding | 0.32-0.83 | VLM描述质量决定上限，纯文本表示图表信息损失大 |
| comparison | 0.36-0.65 | 需要同时检索多个对比维度，融合难度高 |

### 4.5 不足与改进方向

1. **VLM图表提取不完整**：部分图表（如力传感器应用场景饼图）的描述不够详细，导致检索不到正确页面。可尝试使用更强的VLM或增加多轮提取策略。

2. **页面级定位精度不足**：当前分块策略可能导致同一页面的图表和文字被分开，影响精确定位。可考虑引入页面级聚合策略。

3. **LLM答案过长**：v2提示词虽有长度预算，但LLM仍倾向生成长篇分析，导致与简洁标准答案的ROUGE匹配度降低。可加强长度约束或增加摘要后处理。

4. **混合检索融合策略待优化**：RRF的k参数和各通道权重可进一步调优，可尝试学习式融合方法。

5. **Self-RAG效果有限**：当前基于规则的自检方法判断粒度较粗，可考虑引入LLM-based自检。

## 五、总结

本项目实现了一个完整的多模态RAG问答系统，覆盖从PDF解析、多模态理解、混合检索、重排到答案生成的全流程。核心贡献包括：

1. **双层数据架构**：VLM结构化图表描述 + PyMuPDF纯文本，兼顾语义丰富度和处理效率
2. **8种检索器配置**的系统对比，验证了CrossEncoder重排是最关键的性能提升因素
3. **4类问题分类**的针对性提示词工程，提升了不同类型问题的回答质量
4. **多阶段重排流水线**：RRF粗排 → CE精排 → MMR多样性 → 类型过滤

最佳配置（Dense+Rerank）综合得分达到0.6731，文件名准确率95.5%，页面准确率80.6%，ROUGE-1 recall 53.5%。主要瓶颈在于页面级精确定位和图表信息的完整提取，这是未来优化的核心方向。

---

## 附录：团队成员贡献

| 成员 | 主要贡献 |
|------|----------|
| （待填写） | |
| （待填写） | |
| （待填写） | |
